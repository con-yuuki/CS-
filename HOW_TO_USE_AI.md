# AIを使った修正方法（初心者向け）

## 🎯 このガイドの目的

コードを書かなくても、**AIに依頼するだけで修正できる**方法を説明します。

## 🚀 準備

### ステップ1: 開発サーバーを起動

1. Cursorの下部にある「ターミナル」をクリック
2. 以下を入力してEnter：

```bash
npm run dev
```

3. 「Ready」と表示されたら準備完了

### ステップ2: ブラウザで開く

1. ブラウザで `http://localhost:3000` にアクセス
2. 画面が表示されます

## 💬 AIへの依頼方法

### 基本的な流れ

1. **画面を見ながら、何を変更したいか考える**
2. **CursorのAIチャットを開く**（Cmd+L / Ctrl+L）
3. **変更したい内容を説明**
4. **AIが自動的にコードを修正**
5. **ブラウザで結果を確認**

## 📝 依頼例

### 例1: ボタンの色を変更

**画面を見ながら**：
- ホームページの「インポート画面へ」ボタンが青い
- これを緑に変更したい

**AIに依頼**：
```
ホームページの「インポート画面へ」ボタンの色を青から緑に変更してください。
```

**結果**：
- AIが `app/page.tsx` を開く
- ボタンの色を変更
- ファイルを保存
- ブラウザが自動更新され、ボタンが緑色になる

### 例2: テキストを変更

**画面を見ながら**：
- ホームページのタイトルが「CS Health Score Dashboard」
- これを「顧客健康スコア」に変更したい

**AIに依頼**：
```
ホームページのタイトル「CS Health Score Dashboard」を「顧客健康スコア」に変更してください。
```

**結果**：
- AIが `app/page.tsx` を開く
- タイトルを変更
- ファイルを保存
- ブラウザが自動更新され、タイトルが変更される

### 例3: 機能を追加

**画面を見ながら**：
- インポートページに成功メッセージがない
- 成功メッセージを表示したい

**AIに依頼**：
```
インポートページに、データのインポートが成功したときに「インポートが完了しました」というメッセージを表示する機能を追加してください。
```

**結果**：
- AIが `app/import/page.tsx` を開く
- 成功メッセージを表示するコードを追加
- ファイルを保存
- ブラウザが自動更新され、新しい機能が追加される

## 🎨 よくある依頼パターン

### 見た目の変更

```
「[ページ名]の[要素名]の[属性]を[変更前]から[変更後]に変更してください」

例：
「ホームページのボタンの色を青から緑に変更してください」
「ダッシュボードのテーブルのフォントサイズを大きくしてください」
```

### テキストの変更

```
「[ページ名]の[要素名]のテキストを「[変更前]」から「[変更後]」に変更してください」

例：
「ホームページのタイトルを「CS Health Score Dashboard」から「顧客健康スコア」に変更してください」
「インポートページのエラーメッセージを「エラーが発生しました」から「データの読み込みに失敗しました」に変更してください」
```

### 機能の追加

```
「[ページ名]に、[機能の説明]という機能を追加してください」

例：
「インポートページに、データのインポートが成功したときにメッセージを表示する機能を追加してください」
「ダッシュボードに、データをフィルタリングする機能を追加してください」
```

### バグの修正

```
「[ページ名]で、[問題の説明]という問題が発生しています。修正してください」

例：
「インポートページで、ファイルをアップロードしてもエラーが表示される問題を修正してください」
「ダッシュボードで、データが正しく表示されない問題を修正してください」
```

## 💡 依頼のコツ

### 1. 具体的に依頼する

❌ 「ボタンを変更したい」
✅ 「ホームページの「インポート画面へ」ボタンの色を青から緑に変更してください」

### 2. ファイル名を指定する（より確実）

✅ 「ホームページ（app/page.tsx）のボタンの色を変更してください」

### 3. 変更内容を明確にする

❌ 「テキストを変更したい」
✅ 「タイトルを「CS Health Score Dashboard」から「顧客健康スコア」に変更してください」

## 🎯 実践的なワークフロー

### 1. ブラウザで確認

1. `http://localhost:3000` を開く
2. 変更したい箇所を確認
3. スクリーンショットを撮る（必要に応じて）

### 2. AIに依頼

1. CursorのAIチャットを開く（Cmd+L / Ctrl+L）
2. 変更したい内容を説明
3. AIが自動的にコードを修正

### 3. 結果を確認

1. ブラウザが自動更新されます
2. 変更が反映されているか確認
3. 問題があれば、再度AIに依頼

## 📚 参考資料

- `QUICK_FIX_EXAMPLES.md` - よくある修正例（コピー&ペーストで使える）
- `BEGINNER_GUIDE.md` - 初心者向けガイド

---

**コードを書く必要はありません！AIに依頼するだけでOKです！** 🎉

