# 404エラーの解決方法

## 問題

`http://localhost:3000` にアクセスすると「This page could not be found」と表示される。

## 原因

開発サーバーが起動していない可能性があります。

## 解決方法

### ステップ1: 開発サーバーを起動

1. **Cursorのターミナルを開く**
   - 下部にある「ターミナル」をクリック
   - または、`Cmd+J` (Mac) / `Ctrl+J` (Windows)

2. **開発サーバーを起動**
   - ターミナルに以下を入力してEnter：

```bash
cd "/Users/yuuki.takada/Desktop/CSアラート機能"
npm run dev
```

3. **「Ready」と表示されるまで待つ**
   - 通常、数秒で起動します
   - 「Ready」と表示されたら準備完了です

### ステップ2: ブラウザで確認

1. ブラウザで `http://localhost:3000` にアクセス
2. 画面が表示されれば成功です

## よくある問題

### 問題1: ポート3000が既に使用されている

**エラーメッセージ**：
```
Port 3000 is already in use
```

**解決方法**：
1. 既に起動しているサーバーを停止
2. または、別のポートを使用：
   ```bash
   npm run dev -- -p 3001
   ```
3. ブラウザで `http://localhost:3001` にアクセス

### 問題2: 依存関係がインストールされていない

**エラーメッセージ**：
```
Cannot find module '...'
```

**解決方法**：
```bash
npm install
```

その後、再度 `npm run dev` を実行

### 問題3: ビルドエラーが発生している

**エラーメッセージ**：
```
Error: ...
```

**解決方法**：
1. ターミナルのエラーメッセージを確認
2. エラーを修正
3. 再度 `npm run dev` を実行

## 確認方法

### 開発サーバーが起動しているか確認

ターミナルに以下のように表示されていれば、サーバーが起動しています：

```
  ▲ Next.js 14.2.0
  - Local:        http://localhost:3000
  - Ready in 2.3s
```

### ブラウザで確認

1. `http://localhost:3000` にアクセス
2. ホームページが表示されれば成功です

## トラブルシューティング

### それでも404エラーが出る場合

1. **ブラウザのキャッシュをクリア**
   - Ctrl+Shift+R (Windows/Linux)
   - Cmd+Shift+R (Mac)

2. **開発サーバーを再起動**
   - ターミナルで `Ctrl+C` を押して停止
   - 再度 `npm run dev` を実行

3. **`.next` フォルダを削除**
   ```bash
   rm -rf .next
   npm run dev
   ```

---

**開発サーバーを起動すれば、404エラーは解決します！**

