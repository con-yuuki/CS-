# 高度な開発環境（機能・ロジックも含む）

## 概要

見た目だけでなく、**機能やロジックも含めて**「パッと見てパッと修正」できる開発環境を構築します。

## セットアップ

### 1. 開発サーバーを起動

```bash
cd "/Users/yuuki.takada/Desktop/CSアラート機能"
npm run dev
```

### 2. ブラウザで開く

`http://localhost:3000` にアクセス

## リアルタイム開発の流れ

### 機能の修正例

#### 例1: スコア計算ロジックの変更

1. `lib/score-calculator.ts` を開く
2. 計算ロジックを編集
3. 保存（Cmd+S）
4. ブラウザで `/import` ページにアクセス
5. データをインポートして、新しいスコアを確認

#### 例2: バリデーションルールの変更

1. `lib/utils/excel-parser.ts` を開く
2. バリデーションロジックを編集
3. 保存
4. ブラウザで `/import` ページにアクセス
5. ファイルをアップロードして、新しいバリデーションを確認

#### 例3: API呼び出しの修正

1. `lib/services/*.ts` を開く
2. API呼び出しロジックを編集
3. 保存
4. ブラウザで該当ページにアクセス
5. ネットワークタブでAPIリクエストを確認

## デバッグ機能

### 1. コンソールログ

コードに `console.log()` を追加すると、ブラウザのコンソールに表示されます：

```typescript
// 例: スコア計算のデバッグ
console.log("📊 スコア計算:", {
  currentPeriod,
  previousPeriod,
  mrc,
  calculatedScore: score
});
```

### 2. React DevTools

1. [React DevTools](https://react.dev/learn/react-developer-tools) をインストール
2. ブラウザの開発者ツールで「Components」タブを開く
3. コンポーネントの状態を視覚的に確認
4. プロップスやステートをリアルタイムで変更可能

### 3. ネットワークタブ

1. F12キーで開発者ツールを開く
2. 「Network」タブを選択
3. APIリクエストを確認
4. リクエスト/レスポンスの詳細を確認

### 4. エラーの視覚的表示

- エラーがある場合、ブラウザにエラーメッセージが表示されます
- ターミナルにもエラーメッセージが表示されます
- エラーを修正すると、自動的に再読み込みされます

## 実践的な開発フロー

### ステップ1: 問題を特定

1. ブラウザで動作を確認
2. コンソールでエラーを確認
3. ネットワークタブでAPIリクエストを確認

### ステップ2: コードを修正

1. 該当ファイルを開く
2. 問題箇所を修正
3. 保存

### ステップ3: 結果を確認

1. ブラウザが自動更新
2. 修正が反映されているか確認
3. 必要に応じて、コンソールログで詳細を確認

## 便利な開発ツール

### 1. 型チェック

```bash
npm run build
```

TypeScriptの型エラーを確認できます。

### 2. リンター

```bash
npm run lint
```

コードの品質をチェックできます。

### 3. ホットリロード

- ファイルを保存すると、自動的にブラウザが更新されます
- ページの状態（フォーム入力など）は保持されます

## デバッグのコツ

### 1. 段階的に確認

1. まず、コンソールログでデータを確認
2. 次に、UIに表示して確認
3. 最後に、実際の動作を確認

### 2. エラーメッセージを読む

- ターミナルのエラーメッセージを確認
- ブラウザのコンソールのエラーメッセージを確認
- エラーメッセージから問題箇所を特定

### 3. 小さく変更して確認

- 一度に大きな変更をせず、小さく変更して確認
- 動作確認しながら進める

## よくある開発シナリオ

### シナリオ1: スコア計算の調整

1. `lib/score-calculator.ts` を開く
2. 計算ロジックを編集
3. 保存
4. `/import` でデータをインポート
5. `/dashboard` で新しいスコアを確認

### シナリオ2: バリデーションの追加

1. `lib/utils/excel-parser.ts` を開く
2. バリデーションロジックを追加
3. 保存
4. `/import` でファイルをアップロード
5. 新しいバリデーションを確認

### シナリオ3: API呼び出しの最適化

1. `lib/services/*.ts` を開く
2. API呼び出しを最適化
3. 保存
4. ブラウザで動作を確認
5. ネットワークタブでリクエストを確認

---

**これで、機能やロジックも含めて、パッと見てパッと修正できる環境が整いました！**

