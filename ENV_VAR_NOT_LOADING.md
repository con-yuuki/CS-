# 環境変数が読み込まれない問題の解決方法

## 問題

「Supabase環境変数が設定されていません。.env.localファイルを確認してください。」というメッセージが表示される。

## 重要なポイント

**`.env.local`ファイルはローカル開発用です。Vercelの本番環境では使用されません。**

Vercelでは、ダッシュボードで設定した環境変数が使用されます。

## 原因

環境変数はVercelに設定されていますが、**再デプロイを実行していない**ため、ビルド時に環境変数が注入されていません。

Next.jsでは、`NEXT_PUBLIC_`で始まる環境変数は、**ビルド時に**JavaScriptバンドルに埋め込まれます。そのため、環境変数を追加・変更した後は、必ず再デプロイ（再ビルド）が必要です。

## 解決方法

### ステップ1: 再デプロイを実行

環境変数を設定した後は、**必ず再デプロイ**が必要です：

1. [Vercel Dashboard](https://vercel.com/dashboard) にアクセス
2. プロジェクト `alert` を選択
3. 「**Deployments**」タブをクリック
4. 最新のデプロイメントの右側にある「**...**」（三点メニュー）をクリック
5. 「**Redeploy**」を選択
6. 「**Redeploy**」ボタンをクリック
7. デプロイが完了するまで待つ（通常1-2分）

### ステップ2: デプロイログを確認

再デプロイ中に、デプロイログを確認してください：

1. デプロイメントをクリック
2. 「**Build Logs**」を確認
3. エラーがないか確認

### ステップ3: 環境変数が正しく設定されているか確認

1. 「Settings」→「Environment Variables」に移動
2. 以下の2つが存在するか確認：
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
3. 各環境変数の「Environment」列で、Production, Preview, Development すべてにチェックが入っているか確認

### ステップ4: 確認

再デプロイが完了したら：

1. デプロイされたURLにアクセス
2. ブラウザのキャッシュをクリア（Ctrl+Shift+R または Cmd+Shift+R）
3. `/test-connection` ページにアクセス
4. 「接続テストを実行」ボタンをクリック
5. 以下のように表示されればOK：
   - ✅ 環境変数の設定: 正常
   - ✅ Supabase接続: 成功

## よくある質問

### Q: 環境変数は設定したのに、なぜ読み込まれないのですか？

A: **再デプロイを実行していないためです。** Next.jsでは、環境変数はビルド時にJavaScriptバンドルに埋め込まれます。環境変数を追加・変更した後は、必ず再デプロイが必要です。

### Q: `.env.local`ファイルは必要ですか？

A: ローカル開発環境でのみ必要です。Vercelの本番環境では、ダッシュボードで設定した環境変数が使用されます。

### Q: 再デプロイを実行したのに、まだエラーが出ます

A: 以下を確認してください：
1. 環境変数が正しく設定されているか（Production, Preview, Development すべてにチェックが入っているか）
2. 環境変数の値が正しいか
3. ブラウザのキャッシュをクリアしたか
4. デプロイが完了したか（「Ready」と表示されているか）

---

**重要**: 環境変数を設定した後は、必ず再デプロイを実行してください！




