# 本番環境へのデプロイ（簡単手順）

## 最も簡単な方法：Vercel CLIで直接デプロイ

### ステップ1: Vercel CLIをインストール

ターミナルで以下のコマンドを実行してください：

```bash
npm install -g vercel
```

### ステップ2: Vercelにログイン

以下のコマンドを実行すると、ブラウザが開きます：

```bash
vercel login
```

ブラウザでVercelにログインしてください。

### ステップ3: 本番環境にデプロイ

以下のコマンドを実行してください：

```bash
cd "/Users/yuuki.takada/Desktop/CSアラート機能"
vercel --prod
```

これで本番環境にデプロイされます！

## デプロイ後の確認

デプロイが完了すると、URLが表示されます。そのURLにアクセスして動作を確認してください。

## もしエラーが出た場合

エラーメッセージを教えてください。一緒に解決しましょう。

