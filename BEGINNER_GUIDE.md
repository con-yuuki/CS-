# 初心者向けガイド：パッと見てパッと修正

## 🎯 このガイドについて

コードを書くのが初めてでも大丈夫！**画面を見ながら、AIに修正を依頼する**方法を説明します。

## 🚀 まずは開発サーバーを起動

### ステップ1: ターミナルを開く

1. Cursorの下部にある「ターミナル」をクリック
2. または、`Cmd+J` (Mac) / `Ctrl+J` (Windows) で開く

### ステップ2: 開発サーバーを起動

ターミナルに以下を入力してEnter：

```bash
npm run dev
```

### ステップ3: ブラウザで開く

1. ターミナルに「Ready」と表示されたら
2. ブラウザで `http://localhost:3000` にアクセス
3. 画面が表示されます

## 💡 修正の依頼方法

### 方法1: CursorのAIチャットで依頼（最も簡単）

1. **画面を見ながら、何を変更したいか説明**
   - 例：「ボタンの色を青から緑に変更したい」
   - 例：「テキストを変更したい」
   - 例：「レイアウトを調整したい」

2. **AIが自動的にコードを修正**
   - ファイルが自動的に更新されます
   - 保存すると、ブラウザが自動更新されます

3. **結果を確認**
   - ブラウザで変更が反映されているか確認

### 方法2: 具体的な指示を出す

#### 例1: 「ボタンの色を変更したい」

**依頼例**：
```
「ホームページのボタンの色を青から緑に変更してください」
```

**AIが行うこと**：
- `app/page.tsx` を開く
- ボタンの `className` を `bg-blue-500` から `bg-green-500` に変更
- ファイルを保存

**結果**：
- ブラウザが自動更新され、ボタンが緑色になります

#### 例2: 「テキストを変更したい」

**依頼例**：
```
「ホームページのタイトルを「CS Health Score Dashboard」から「顧客健康スコア」に変更してください」
```

**AIが行うこと**：
- `app/page.tsx` を開く
- タイトルのテキストを変更
- ファイルを保存

**結果**：
- ブラウザが自動更新され、タイトルが変更されます

#### 例3: 「機能を追加したい」

**依頼例**：
```
「インポートページに、成功メッセージを表示する機能を追加してください」
```

**AIが行うこと**：
- `app/import/page.tsx` を開く
- 成功メッセージを表示するコードを追加
- ファイルを保存

**結果**：
- ブラウザが自動更新され、新しい機能が追加されます

## 🎨 よくある修正パターン

### パターン1: 見た目の変更

**依頼例**：
- 「ボタンの色を変更したい」
- 「フォントサイズを大きくしたい」
- 「余白を調整したい」
- 「レイアウトを変更したい」

### パターン2: テキストの変更

**依頼例**：
- 「このテキストを変更したい」
- 「エラーメッセージを変更したい」
- 「ボタンのラベルを変更したい」

### パターン3: 機能の追加・変更

**依頼例**：
- 「この機能を追加したい」
- 「この動作を変更したい」
- 「バリデーションを追加したい」

### パターン4: バグの修正

**依頼例**：
- 「このエラーを修正したい」
- 「この動作がおかしいので修正したい」
- 「データが正しく表示されないので修正したい」

## 📝 実践例

### 例1: ボタンの色を変更

1. **ブラウザで確認**
   - `http://localhost:3000` を開く
   - ボタンの色を確認

2. **AIに依頼**
   ```
   「ホームページの「インポート画面へ」ボタンの色を青から緑に変更してください」
   ```

3. **AIが修正**
   - コードが自動的に更新されます

4. **結果を確認**
   - ブラウザが自動更新され、ボタンが緑色になります

### 例2: テキストを変更

1. **ブラウザで確認**
   - 変更したいテキストを確認

2. **AIに依頼**
   ```
   「ホームページの説明文「顧客の健康状態を可視化するダッシュボード」を「顧客の利用状況を可視化するダッシュボード」に変更してください」
   ```

3. **AIが修正**
   - コードが自動的に更新されます

4. **結果を確認**
   - ブラウザが自動更新され、テキストが変更されます

## 🔍 画面を見ながら修正する方法

### ステップ1: ブラウザで確認

1. `http://localhost:3000` を開く
2. 変更したい箇所を確認
3. スクリーンショットを撮る（必要に応じて）

### ステップ2: AIに依頼

1. CursorのAIチャットを開く
2. 変更したい内容を説明
   - 例：「このボタンの色を変更したい」
   - 例：「このテキストを変更したい」
   - 例：「この機能を追加したい」

3. AIが自動的にコードを修正

### ステップ3: 結果を確認

1. ブラウザが自動更新されます
2. 変更が反映されているか確認
3. 問題があれば、再度AIに依頼

## 💬 依頼のコツ

### 良い依頼例

✅ 「ホームページのボタンの色を青から緑に変更してください」
✅ 「インポートページのエラーメッセージを変更してください」
✅ 「ダッシュボードのレイアウトを調整してください」

### より具体的な依頼例

✅ 「ホームページ（`app/page.tsx`）の「インポート画面へ」ボタンの色を `bg-blue-500` から `bg-green-500` に変更してください」
✅ 「インポートページ（`app/import/page.tsx`）の成功メッセージを「インポートが完了しました」に変更してください」

## 🎯 まとめ

1. **開発サーバーを起動**：`npm run dev`
2. **ブラウザで確認**：`http://localhost:3000`
3. **AIに依頼**：変更したい内容を説明
4. **結果を確認**：ブラウザで自動更新を確認

**コードを直接書く必要はありません！AIに依頼するだけでOKです！** 🎉

---

**わからないことがあれば、いつでもAIに質問してください！**

