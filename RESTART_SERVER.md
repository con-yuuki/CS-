# 開発サーバーの再起動方法

## 🔄 サーバーが読み込まなくなった場合

### ステップ1: 既存のサーバーを停止

1. **ターミナルを開く**（Cursorの下部）
2. **Ctrl+C** を押して、既存のサーバーを停止

### ステップ2: サーバーを再起動

ターミナルに以下を入力してEnter：

```bash
cd "/Users/yuuki.takada/Desktop/CSアラート機能"
npm run dev
```

### ステップ3: 「Ready」と表示されるまで待つ

ターミナルに以下のように表示されます：

```
  ▲ Next.js 14.2.0
  - Local:        http://localhost:3000
  ✓ Ready in 2.3s
```

## 🚨 よくある問題と解決方法

### 問題1: エラーが表示される

**解決方法**：
1. ターミナルのエラーメッセージを確認
2. エラーを修正
3. 再度 `npm run dev` を実行

### 問題2: ポートが使用されている

**解決方法**：
1. 既存のサーバーを停止（Ctrl+C）
2. 再度起動：
   ```bash
   npm run dev
   ```

### 問題3: 依存関係の問題

**解決方法**：
1. 依存関係を再インストール：
   ```bash
   npm install
   ```
2. サーバーを起動：
   ```bash
   npm run dev
   ```

### 問題4: キャッシュの問題

**解決方法**：
1. `.next` フォルダを削除：
   ```bash
   rm -rf .next
   ```
2. サーバーを起動：
   ```bash
   npm run dev
   ```

## ✅ 確認方法

### ターミナルで確認

以下のように表示されていれば、サーバーが起動しています：

```
✓ Compiled successfully
  ▲ Next.js 14.2.0
  - Local:        http://localhost:3000
  ✓ Ready in 2.3s
```

### ブラウザで確認

1. `http://localhost:3000` にアクセス
2. ホームページが表示されれば成功です

---

**サーバーを再起動すれば、読み込めるようになります！**




