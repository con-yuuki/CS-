# デプロイ完了確認方法

## 環境変数設定後の確認手順

### ステップ1: 環境変数が正しく設定されているか確認

1. Vercelダッシュボードで、プロジェクト `alert` を開く
2. 「Settings」→「Environment Variables」に移動
3. 以下の2つの環境変数が表示されているか確認：
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### ステップ2: 再デプロイを実行（重要！）

環境変数を追加・変更した後は、**必ず再デプロイが必要**です：

1. Vercelダッシュボードで「**Deployments**」タブをクリック
2. 最新のデプロイメントの右側にある「**...**」（三点メニュー）をクリック
3. 「**Redeploy**」を選択
4. 「**Redeploy**」ボタンをクリック
5. デプロイが完了するまで待つ（通常1-2分）

### ステップ3: デプロイされたアプリケーションで確認

1. デプロイされたURLにアクセス（例: `https://alert-teal.vercel.app`）
2. 「**/import**」ページにアクセス
3. 環境変数が正しく設定されている場合：
   - ⚠️ 警告メッセージが表示されない
   - インポートボタンが有効になっている

4. 「**/test-connection**」ページにアクセス
5. 「**接続テストを実行**」ボタンをクリック
6. 以下のように表示されればOK：
   - ✅ 環境変数の設定: 正常
   - ✅ Supabase接続: 成功
   - ✅ データベーステーブル: すべて存在

## デプロイが完了したかどうかの確認

### Vercelダッシュボードで確認

1. 「**Deployments**」タブを開く
2. 最新のデプロイメントの状態を確認：
   - ✅ **Ready**: デプロイ完了
   - ⏳ **Building**: デプロイ中
   - ❌ **Error**: エラーが発生

### デプロイログを確認

1. デプロイメントをクリック
2. 「**Build Logs**」または「**Function Logs**」を確認
3. エラーがないか確認

## トラブルシューティング

### 環境変数が反映されない場合

1. **再デプロイを実行**: 環境変数を追加・変更した後は、必ず再デプロイが必要です
2. **環境の確認**: Production, Preview, Developmentすべてに設定されているか確認
3. **変数名の確認**: `NEXT_PUBLIC_` で始まっているか確認

### インポート機能が動作しない場合

1. **ブラウザのコンソールを確認**: F12キーを押して、エラーメッセージを確認
2. **接続テストを実行**: `/test-connection` ページで接続状態を確認
3. **環境変数の再確認**: Vercelダッシュボードで環境変数が正しく設定されているか確認

## 次のステップ

デプロイが完了したら：

1. ✅ デプロイされたURLにアクセス
2. ✅ `/import` ページでインポート機能をテスト
3. ✅ `/test-connection` ページで接続を確認
4. ✅ 実際のExcelファイルでインポートを試す

---

**デプロイが完了したら、インポート機能を試してみてください！**

