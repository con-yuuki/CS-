# ビジュアル開発ガイド（Gemini Canvas風）

## 🎨 パッと見てパッと修正できる開発環境

### セットアップ（初回のみ）

1. **環境変数は既に設定済み**（`.env.local`ファイルが存在します）

2. **開発サーバーを起動**：

```bash
cd "/Users/yuuki.takada/Desktop/CSアラート機能"
npm run dev
```

または、簡単なスクリプトを使用：

```bash
./start-dev.sh
```

3. **ブラウザで開く**：
   - `http://localhost:3000` にアクセス

## 🔥 リアルタイム編集の流れ

### 1. 2画面構成で作業

```
┌─────────────────┬─────────────────┐
│  コードエディタ  │     ブラウザ     │
│   (Cursor)      │  localhost:3000 │
│                 │                 │
│  ファイル編集    │  結果を確認      │
│  ↓             │  ↓             │
│  保存 (Cmd+S)   │  自動更新！      │
└─────────────────┴─────────────────┘
```

### 2. 編集→保存→確認のサイクル

1. **コードを編集**（例：`app/page.tsx`）
2. **保存**（Cmd+S / Ctrl+S）
3. **数秒で自動更新**（ブラウザが自動リロード）
4. **結果を確認**（ブラウザで即座に確認）

### 3. ホットリロードの確認

- ファイルを保存すると、ターミナルに「✓ Compiled successfully」と表示
- ブラウザが自動的に更新されます
- **ページをリロードする必要はありません**

## 🛠️ よく編集するファイル

### UIの見た目を変更

- **ホームページ**: `app/page.tsx`
- **インポートページ**: `app/import/page.tsx`
- **ダッシュボード**: `app/dashboard/page.tsx`
- **トレンドページ**: `app/trends/page.tsx`
- **UIコンポーネント**: `components/ui/*.tsx`

### スタイルを変更

- **グローバルスタイル**: `app/globals.css`
- **Tailwind設定**: `tailwind.config.ts`
- **各ページ**: ファイル内の `className` を編集

### ロジックを変更

- **スコア計算**: `lib/score-calculator.ts`
- **データ処理**: `lib/services/*.ts`
- **バリデーション**: `lib/utils/excel-parser.ts`

## 🎯 実践例

### 例1: ボタンの色を変更

1. `app/page.tsx` を開く
2. ボタンの `className` を編集（例：`bg-blue-500` → `bg-green-500`）
3. 保存
4. ブラウザで即座に確認

### 例2: テキストを変更

1. 任意のページファイルを開く
2. テキストを編集
3. 保存
4. ブラウザで即座に確認

### 例3: レイアウトを調整

1. `app/globals.css` または `tailwind.config.ts` を編集
2. 保存
3. ブラウザで即座に確認

## 🔍 デバッグ機能

### ブラウザの開発者ツール

1. **F12キー**で開発者ツールを開く
2. **Elements**タブ：
   - HTMLを確認
   - スタイルを一時的に編集（リロードすると元に戻る）
3. **Console**タブ：
   - `console.log()` の出力を確認
   - エラーメッセージを確認
4. **Network**タブ：
   - APIリクエストを確認
   - レスポンスを確認

### コンソールログの追加

コードに `console.log()` を追加すると、ブラウザのコンソールに表示されます：

```typescript
console.log("デバッグ情報:", data);
```

## ⚡ 便利なショートカット

### Cursor（エディタ）

- **Cmd+S / Ctrl+S**: 保存（自動リロード）
- **Cmd+P / Ctrl+P**: ファイルを開く
- **Cmd+Shift+F / Ctrl+Shift+F**: 全体検索

### ブラウザ

- **Cmd+R / Ctrl+R**: 通常のリロード
- **Cmd+Shift+R / Ctrl+Shift+R**: ハードリロード（キャッシュクリア）
- **F12**: 開発者ツールを開く

## 🚨 トラブルシューティング

### ホットリロードが動作しない

1. ブラウザのキャッシュをクリア（Cmd+Shift+R）
2. 開発サーバーを再起動（Ctrl+C で停止 → `npm run dev`）
3. `.next` フォルダを削除：
   ```bash
   rm -rf .next
   npm run dev
   ```

### エラーが表示される

1. **ターミナルを確認**：エラーメッセージが表示されます
2. **ブラウザのコンソールを確認**：F12キーで開く
3. **エラーを修正**：自動的に再読み込みされます

## 📦 本番環境へのデプロイ前の確認

### 1. ローカルでビルドテスト

```bash
npm run build
```

エラーがないか確認します。

### 2. 本番環境と同じ状態で確認

```bash
npm run build
npm run start
```

`http://localhost:3000` で本番環境と同じ状態を確認できます。

### 3. 問題なければデプロイ

```bash
git add .
git commit -m "変更内容"
git push
```

Vercelが自動的にデプロイします。

---

**これで、GeminiのCanvas機能のように、パッと見てパッと修正できる環境が整いました！** 🎨

