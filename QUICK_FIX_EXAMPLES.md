# よくある修正例（コピー&ペーストで使える）

## 🎯 使い方

このファイルの依頼例をコピーして、CursorのAIチャットに貼り付けてください。

## 📝 見た目の変更

### ボタンの色を変更

```
ホームページ（app/page.tsx）の「インポート画面へ」ボタンの色を青から緑に変更してください。
```

### テキストの色を変更

```
ホームページ（app/page.tsx）のタイトルの色を黒から青に変更してください。
```

### フォントサイズを変更

```
ホームページ（app/page.tsx）のタイトルのフォントサイズを大きくしてください。
```

### 余白を調整

```
ホームページ（app/page.tsx）のカードの間隔を広げてください。
```

## 📝 テキストの変更

### タイトルを変更

```
ホームページ（app/page.tsx）のタイトル「CS Health Score Dashboard」を「顧客健康スコア」に変更してください。
```

### 説明文を変更

```
ホームページ（app/page.tsx）の説明文「顧客の健康状態を可視化するダッシュボード」を「顧客の利用状況を可視化するダッシュボード」に変更してください。
```

### ボタンのラベルを変更

```
ホームページ（app/page.tsx）の「インポート画面へ」ボタンのラベルを「データをインポート」に変更してください。
```

## 📝 機能の追加

### 成功メッセージを追加

```
インポートページ（app/import/page.tsx）に、データのインポートが成功したときに「インポートが完了しました」というメッセージを表示する機能を追加してください。
```

### 確認ダイアログを追加

```
インポートページ（app/import/page.tsx）に、インポート実行前に「本当にインポートしますか？」という確認ダイアログを表示する機能を追加してください。
```

### ローディング表示を追加

```
インポートページ（app/import/page.tsx）に、インポート処理中に「処理中...」というローディング表示を追加してください。
```

## 📝 バグの修正

### エラーメッセージを修正

```
インポートページ（app/import/page.tsx）で、エラーが発生したときに表示されるメッセージを、より分かりやすい内容に変更してください。
```

### データの表示を修正

```
ダッシュボード（app/dashboard/page.tsx）で、データが正しく表示されない問題を修正してください。
```

### バリデーションを修正

```
インポートページ（app/import/page.tsx）のバリデーションロジックを修正して、より正確にエラーを検出できるようにしてください。
```

## 📝 レイアウトの調整

### カードの配置を変更

```
ホームページ（app/page.tsx）のカードを、横並びから縦並びに変更してください。
```

### サイドバーを追加

```
ダッシュボード（app/dashboard/page.tsx）に、左側にサイドバーを追加してください。
```

### レスポンシブ対応

```
ホームページ（app/page.tsx）を、スマートフォンでも見やすく表示されるように調整してください。
```

## 💡 使い方のコツ

1. **具体的に依頼する**
   - ❌ 「ボタンを変更したい」
   - ✅ 「ホームページの「インポート画面へ」ボタンの色を青から緑に変更してください」

2. **ファイル名を指定する**
   - ✅ 「ホームページ（app/page.tsx）の...」

3. **変更内容を明確にする**
   - ✅ 「色を青から緑に変更」
   - ✅ 「テキストを「XXX」から「YYY」に変更」

---

**この依頼例をコピー&ペーストして、AIに依頼してください！**

